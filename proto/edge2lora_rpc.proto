syntax = "proto3";

package edge2lorarpcservice;

service Edge2LoraRPCService {
    rpc new_data (NewDataRequest) returns (NewDataResponse);
}

message NewDataRequest {
   string name = 1;
   uint64 timetag = 2;
}

message NewDataResponse {
    string message = 1;
}